services:
  - type: web
    name: gsc-announcement-backend
    env: node
    # keep rootDir so Render can operate in the backend folder when supported
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
